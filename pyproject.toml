[project]
name = "javali-tools"
version = "1.0.5"
description = "Javali Red Team Toolkit"
authors = [{name="Sylvain Julio"}]
dependencies = [
    "tabulate",
    "termcolor",
    "tqdm",
    "cryptography",
    "pwn"
]

[project.scripts]
javali_tools = "javali_tools.cli:main"
